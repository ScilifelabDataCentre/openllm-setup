apiVersion: v1
kind: Service
metadata:
  name: {{ include "embeddings.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "embeddings.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "embeddings.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
