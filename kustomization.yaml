apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: charts

helmCharts:
  - name: open-webui
    releaseName: open-webui
    namespace: llm-stack
    version: 0.1.0
    valuesFile: values.yaml

resources:
  - resources/tailscale-auth.ss.yaml
