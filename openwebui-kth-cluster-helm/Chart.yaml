apiVersion: v2
name: openwebui-kth-cluster-helm
description: Open WebUI deployment with bundled PostgreSQL, Qdrant, and vLLM embeddings.
type: application
version: 0.2.0
appVersion: "main"
dependencies:
  - name: postgresql
    version: 18.6.1
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
